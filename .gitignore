node_modules/
dist/
.venv/
__pycache__/
*.py[cod]

# Generated runtime files and private source footage.
public/mediapipe/
docs/practitioner-footage/

# Generated from the pinned MediaPipe npm package by the WASM copy script.
public/mediapipe/

# Practitioner source material stays in approved local/restricted storage. Commit
# only consented, anonymized landmark fixtures derived from it.
docs/practitioner-footage/

# NOTE: public/guides/ is deliberately NOT ignored. The three visitor-facing guide clips
# are committed so a checkout can run the booth without access to the restricted footage.
# They are cleared, cut-down derivatives — not the raw session material, which stays out
# of this repository. Regenerate them with scripts/build_practitioner_guides.mjs.

coverage/
playwright-report/
test-results/
local-artifacts/
.vite/
*.local
*.log
~$*.docx
.env
.env.*
!.env.example
